This folder contains TypeScript sources for the Financy backend. Use `npm run dev` to start in development.

Remember to create `.env` using the `.env.example` file and configure `DATABASE_URL` for your local PostgreSQL.
