# Docker-based Postgres example (recommended)
DATABASE_URL="postgresql://financy:financy_pass@localhost:5432/financy"
PORT=3333

# If you run Postgres in Docker the `docker-compose.yml` maps port 5432,
# so keep host as `localhost`. Adjust credentials if you change them in compose.
